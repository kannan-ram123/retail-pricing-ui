
export const environment = {
  production: true,
  // Replace with real backend URL for production (include scheme and port)
  apiBaseUrl: 'https://api.example.com' // <- update for your deployment
};